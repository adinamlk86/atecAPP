import { ClientTextFilterPipe } from './client-text-filter.pipe';

describe('ClientTextFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ClientTextFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
